describe('src/index.ts', () => {
  describe('when trigger event is "push"', () => {
    it.todo('triggers a push flow')
    it.todo('catches errors thrown by the push flow implementation')
  })

  describe('when trigger event is "pull_request"', () => {
    it.todo('triggers a pull-request flow')
    it.todo('catches errors thrown by the pull-request flow implementation')
  })
})
